<!DOCTYPE html>
<html>
  <head>
    <title>Snacky</title>
    <link rel="icon" type="image/x-icon" href="https://res.cloudinary.com/le-wagon-785-vchang/image/upload/v1663937239/production/snacky-assets/favicon_a4zd8p.ico">
    <link rel="apple-touch-icon" href="https://res.cloudinary.com/le-wagon-785-vchang/image/upload/v1664474206/production/snacky-assets/logo_200x200_dqyxjz.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@200;300;400;500;600;900&family=Rubik:ital,wght@0,400;0,500;1,900&display=swap" rel="stylesheet">
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.8.2/mapbox-gl.js"></script>
  </head>

  <body>
    <%= render "shared/flashes" %>
    <%= render "shared/javascript_flashes" %>

    <%# <% unless ((user_signed_in? == false) && current_page?(new_user_session_path)) || ((user_signed_in? == false) && current_page?(new_user_registration_path)) || action_name == "faq" || action_name == "home"  %>
      <%# <%= render 'shared/navbar' %>
      <%# if user_signed_in? %>
    <%# <% end %>

    <%= yield %>
  </body>
</html>
